import 'package:flutter/widgets.dart';

class TotalAmountCardModel {
  String? title;
  IconData? icon;
  Color ? color ;
  bool ? showDropDownMenu ;
  bool ? showDebtCheck ;

  TotalAmountCardModel({required this.icon, required this.title,required this.color,required this.showDropDownMenu,required this.showDebtCheck});
}
